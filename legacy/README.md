# Legacy implementations

The maintained simulator lives in `oe_tc/`. The code in this directory is
retained only as historical reference and is neither packaged nor tested.

- `previous_model/` is the direct predecessor that formerly occupied the
  repository root.
- `early_prototype/` contains the still earlier experimental implementation.

Compatibility with current dependencies is not guaranteed.